---
title: "Session 11 lab: functions"
date: "`r format(Sys.time(), '%Y-%m-%d %H:%M:%S %Z')`"
output: html_document
---

```{r}
markers <- data.frame(
  marker_a = c(4.1, 5.2, NA, 6.0),
  marker_b = c(12, 10, 9, 11),
  marker_c = c(NA, NA, NA, NA)
)
baseline <- c(10, 8, NA, 12)
followup <- c(8, 9, 7, 12)
```

## 1. Safe mean

```{r}
safe_mean <- function(x) {
  # Validate and calculate.
}
```

## 2. Iterate over columns

```{r}
# Preallocate a named numeric vector, then fill it.
```

## 3. Classify change

```{r}
classify_change <- function(change) {
  # Return "decrease", "no decrease", or "missing" elementwise.
}
```

## 4. Refactor and test

```{r}
# Define change_summary(baseline, followup), then test edge cases.
```

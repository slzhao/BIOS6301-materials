---
title: "Session 13 lab: iteration"
format: html
---

```{r}
set.seed(6301)
simulated <- as.data.frame(replicate(12, rnorm(200, mean = 0.25)))
names(simulated) <- paste0("sim", seq_along(simulated))
```

## 1. One operation

```{r}
one_summary <- function(x) {
  # Return named mean, SD, and proportion > 0.
}
```

## 2. Loop and functional

```{r}
# Apply one_summary with a loop, then vapply or lapply.
```

## 3. Coercion demonstration

```{r}
# Add a character column and inspect as.matrix(simulated).
```

## 4. Measurement and recommendation

```{r}
# Compare growing and preallocated output on representative work.
```

Write your recommendation here.

---
title: "Session 16 lab: strings"
format: html
---

```{r}
medication <- data.frame(
  id = sprintf("P%02d", 1:8),
  text = c("Metformin 500 mg twice daily", "metformin 1,000mg qd",
           "METFORMIN 850 mg BID", "No medications", NA,
           "metformin dose unknown", "500 mL saline daily", "metformin 250 mg daily")
)
```

## 1. Normalize and classify

```{r}
# Preserve text; create normalized and documentation-status fields.
```

## 2. Extract dose and frequency

```{r}
# Use base R or stringr and retain unparsed records for review.
```

## 3. Pattern tests

```{r}
# Add positive and negative cases with expected results.
```

## 4. Limitations

Write two reasons this parser is not ready for unrestricted clinical text.

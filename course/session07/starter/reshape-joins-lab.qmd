---
title: "Session 7 lab: reshaping and joins"
date: now
date-format: "YYYY-MM-DD HH:mm:ss Z"
format: html
---

```{r}
library(dplyr)
library(tidyr)

participants <- data.frame(
  id = c("P01", "P02", "P02", "P03", "P04"),
  arm = c("control", "treated", "treated", "treated", "control"),
  record_status = c("final", "draft", "final", "final", "final")
)
markers_wide <- data.frame(
  id = c("P01", "P02", "P03", "P05"),
  marker_w0 = c(4.1, 5.2, 6.0, 4.8),
  marker_w4 = c(4.4, 4.7, 5.1, 4.9)
)
```

Adjudication rule: when an ID has both draft and final metadata, retain the final
record. `P05` intentionally has no participant record.

## 1. Key audit and resolution

```{r}
# Use count() to find duplicate IDs, then apply the stated adjudication rule.
```

## 2. Reshape and join

```{r}
# Use pivot_longer(), parse week as integer, and request a many-to-one join.
```

## 3. Validate and calculate change

```{r}
# Check row count, duplicate keys, and unmatched IDs. Then arrange, group, and lag.
```

## 4. Round-trip check

```{r}
# Return to wide and compare the marker columns with markers_wide.
```

## 5. Additional practice

```{r}
# Continue with any additional-practice tasks assigned during lab.
```

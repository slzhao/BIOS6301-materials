---
title: "Session 12 lab: debugging"
date: "`r format(Sys.time(), '%Y-%m-%d %H:%M:%S %Z')`"
output: html_document
---

```{r}
events_treated <- c(4, 2, NA, 3)
time_treated <- c(40, 25, 30, 0)
events_control <- c(5, 4, 3)
time_control <- c(42, 27, 31)
```

## Broken function

```{r}
risk_ratio <- function(e1, t1, e0, t0) {
  mean(e1 / t1) / mean(e0 / t0)
}
```

## 1. Reproduce and diagnose

```{r}
# Call the function and inspect intermediate rates.
```

## 2. Repair and validate

```{r}
# Define a documented missing/zero-time policy and enforce it.
```

## 3. Tests and minimal reprex

```{r}
# Add ordinary, missing, boundary, and invalid-input tests.
```

Explain why `(0.1 + 0.2) == 0.3` is not a suitable test.

---
title: "Session 14 lab: simulation"
date: "`r format(Sys.time(), '%Y-%m-%d %H:%M:%S %Z')`"
output: html_document
---

## ADEMP

Write the aims, data-generating mechanism, estimand, methods, and performance
measures here.

```{r}
simulate_one <- function(n = 30, delta = 0, sd_control = 1, sd_treated = 2) {
  # Generate two groups.
  # Run Welch and equal-variance t tests.
  # Return the estimate and both p-values as a named numeric vector.
}
```

## One-replicate validation

```{r}
set.seed(6301)
# Inspect one result and test its names, type, and length.
```

## Scenarios

```{r}
# Run delta = 0, 0.3, and 0.6; retain parameters and calculate MCSE.
```

## Interpretation

State the precision target, failures, and the conditions under which the results
apply.

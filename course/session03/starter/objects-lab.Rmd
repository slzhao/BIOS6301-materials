---
title: "Session 3 lab: R objects"
date: "`r format(Sys.time(), '%Y-%m-%d %H:%M:%S %Z')`"
output: html_document
---

```{r}
patient_id <- sprintf("P%02d", 1:8)
arm_text <- c("treated", "control", "treated", "control",
              "treated", "control", "treated", "control")
dose_mg <- c("10", "0", "not measured", "0", "20", "0", "10", "0")
```

## 1. Inspect

```{r}
# Add inspection code.
```

## 2. Diagnose and repair dose

```{r}
# Create dose_clean without overwriting dose_mg.
```

## 3. Name and subset the vector

```{r}
# Give dose_clean patient-ID names and select complete treated observations.
```

## 4. Summarize, classify, and interpret

```{r}
# Report counts and the mean, then classify each dose with ifelse().
```

Write your interpretation here.

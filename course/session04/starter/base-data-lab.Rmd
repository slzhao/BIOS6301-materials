---
title: "Session 4 lab: base-R data handling"
date: "`r format(Sys.time(), '%Y-%m-%d %H:%M:%S %Z')`"
output: html_document
---

```{r}
visit <- data.frame(
  id = rep(sprintf("P%02d", 1:6), each = 2),
  arm = rep(rep(c("control", "treated"), each = 3), each = 2),
  week = rep(c(0, 4), 6),
  marker = c(4.1, 4.4, 5.0, NA, 4.6, 4.8,
             5.2, 4.6, 6.0, 5.1, 5.6, NA)
)
```

## 1. Row unit, key, and structures

```{r}
# Inspect and compare the three marker extractions.
```

## 2. Week-4 analysis data

```{r}
# Select rows and add observed/change variables.
```

## 3. Arm summary and matrix

```{r}
# Use split/lapply or another transparent base-R approach.
```

## 4. Checks

```{r}
# Encode at least three required properties with stopifnot().
```

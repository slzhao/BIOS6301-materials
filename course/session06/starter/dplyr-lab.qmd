---
title: "Session 6 lab: tidy data and dplyr"
format: html
---

```{r}
visit <- data.frame(
  id = rep(sprintf("P%02d", 1:6), each = 2),
  arm = rep(rep(c("control", "treated"), each = 3), each = 2),
  week = rep(c(0, 4), 6),
  marker = c(4.1, 4.4, 5.0, NA, 4.6, 4.8,
             5.2, 4.6, 6.0, 5.1, 5.6, NA)
)
```

## 1. Validate structure and key

```{r}
# Confirm row meaning and key uniqueness.
```

## 2. Analysis population and derived variables

Write the inclusion rule here.

```{r}
# Use dplyr or base R.
```

## 3. Arm summary and centered values

```{r}
# Report n, observed n, mean, SD, and proportion observed.
```

## 4. Cross-dialect verification

```{r}
# Verify observed counts using the other dialect.
```

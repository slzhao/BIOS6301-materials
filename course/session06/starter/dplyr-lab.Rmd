---
title: "Session 6 lab: tidy data and dplyr"
date: "`r format(Sys.time(), '%Y-%m-%d %H:%M:%S %Z')`"
output: html_document
---

```{r}
library(dplyr)

visit <- data.frame(
  id = rep(sprintf("P%02d", 1:6), each = 2),
  arm = rep(rep(c("control", "treated"), each = 3), each = 2),
  week = rep(c(0, 4), 6),
  marker = c(4.1, 4.4, 5.0, NA, 4.6, 4.8,
             5.2, 4.6, 6.0, 5.1, 5.6, NA)
)
```

## 1. Validate structure and key

```{r}
# State the row unit in prose, then use count() and filter() to inspect duplicates.
```

## 2. Analysis population and derived variables

Write the inclusion rule here.

```{r}
# Filter to week 4. Create observed and marker_band with mutate() and case_when().
```

## 3. Arm summary

```{r}
# Use group_by(), summarise(), and .groups = "drop".
```

## 4. Grouped transformation and review table

```{r}
# Center marker within arm, ungroup, arrange, and select review columns.
```

## 5. Base-R verification

```{r}
# Verify the arm-specific observed counts with tapply().
```

## 6. Additional practice

```{r}
# Continue with any additional-practice tasks assigned during lab.
```
